import BaseModule from '@/store/base-module';
import AreaService from "@/services/AreaService";

var base = new BaseModule(AreaService);


const state = {
  ...base.state,
};
const getters = {
  ...base.getters,
};
const actions = {
  ...base.actions,
};
const mutations = {
  ...base.mutations,
};


export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};