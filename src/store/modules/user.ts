import BaseModule from '@/store/base-module';
import UserService from "@/services/UserService";

var base = new BaseModule(UserService);


const state = {
  ...base.state,
};
const getters = {
  ...base.getters,
};
const actions = {
  ...base.actions,
};
const mutations = {
  ...base.mutations,
};


export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
