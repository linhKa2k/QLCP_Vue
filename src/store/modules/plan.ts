import BaseModule from '@/store/base-module';
import PlanService from "@/services/PlanService";

var base = new BaseModule(PlanService);


const state = {
  ...base.state,
};
const getters = {
  ...base.getters,
};
const actions = {
  ...base.actions,
};
const mutations = {
  ...base.mutations,
};


export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};